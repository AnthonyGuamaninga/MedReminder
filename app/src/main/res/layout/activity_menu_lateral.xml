<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout_activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    >

    <!-- Aquí va tu layout actual -->
    <include
        android:id="@+id/activity_menu_lateral"
        layout="@layout/activity_vistaprincipal" />

    <!-- Aquí agregas el NavigationView -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"

        android:background="@color/fondo1_cyan"

        app:itemTextColor="@color/black"
        app:menu="@menu/navigation_menu_bar" />

</androidx.drawerlayout.widget.DrawerLayout>
